<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tier 0 - kernel (docs/architecture/modules.md 3).

    Money, Quantity, Percentage, DateRange, strongly-typed id primitives, Result/Error. See
    README.md in this folder for what each type is and the decisions behind it.

    This assembly references nothing but the BCL. Do not add a PackageReference or a
    ProjectReference here: SharedKernelAssemblyTests asserts both that and the absence of any
    double/float, and B-04 adds the solution-wide fitness rules L1 and F1 over the same ground.
  -->

  <PropertyGroup>
    <RootNamespace>Aurora.SharedKernel</RootNamespace>
  </PropertyGroup>

</Project>
