# Backlog

Owned by the project-manager. The orchestrator updates the Status column as work moves.

Statuses: `draft`, `ready`, `in-progress`, `review`, `changes-requested`, `done`, `blocked`.

**How to read "ready" here.** Every row below marked `ready` is fully specified and scoped to fit one developer session, per the project-manager role's definition of ready. The **Depends on** column is authoritative for actual dispatch order — most rows below depend on `B-01` directly or transitively, and since nothing is `done` yet at bootstrap, only `B-01` is literally unblocked right now. The rest are marked `ready` (rather than `draft`) because their specs are complete and nothing further needs grooming before a developer can pick them up **the moment their dependency reaches `done`** — this is what "keep at least 5 ready tasks ahead of the developers" (the project-manager role) means in practice: the queue never runs dry waiting on the project-manager. The orchestrator should not dispatch a `ready` task ahead of an incomplete dependency; it should treat this table as the next several waves of work, groomed in advance.

Bootstrap tasks `B-01` … `B-15` are the architect's walking-skeleton sequence (`docs/architecture/solution-layout.md` §6), reproduced here with real dependency ordering and, where the task is pure platform/engineering scaffolding with no product-facing "problem and user," the architect's own acceptance criteria stand as the spec (see Spec column) rather than a duplicate product document — a PM spec exists only where the task has real business/tenancy/permission/audit shape a developer would otherwise have to guess at (`B-07`, `B-15`). `B-15` itself has been split into three backlog rows (`B-15.1`/`.2`/`.3`) because, as specified, it bundles domain+application+infrastructure+API+UI+two-tenant testing into one task that does not fit a single session — this is the project-manager splitting an oversized task per its own role guidance, not a redesign of the architect's sequence; all three still map to the architect's single `B-15` acceptance criteria collectively.

| ID | Title | Spec | Module | Depends on | Parallel-safe | Status | Notes |
|---|---|---|---|---|---|---|---|
| B-01 | Solution skeleton and build-wide settings | `architecture/solution-layout.md` §6 (B-01) | Solution/Build | — | Yes — nothing else can start until this lands, but nothing blocks *it* | ready | Pure engineering scaffolding; architect's AC is the spec. Everything else in this table depends on it, directly or transitively. |
| B-02 | `scripts/verify.sh` stages 0–3 and 11 | `architecture/solution-layout.md` §6 (B-02), §5 | Build/Tooling | B-01 | Yes — with B-03, B-05 | ready | Touches `scripts/` only; no file overlap with B-03/B-05. |
| B-03 | `Aurora.SharedKernel` (Money, Quantity, Percentage, DateRange, ids, `Result`) | `architecture/solution-layout.md` §6 (B-03) | SharedKernel (tier 0) | B-01 | Yes — with B-02, B-05 | ready | Foundational value objects; every later module reads this but nothing here reads them back. |
| B-04 | `Aurora.Architecture.Tests` first rule set | `architecture/solution-layout.md` §6 (B-04) | Architecture.Tests | B-01, B-03 | Yes — with B-05, B-12 | ready | Needs B-03 to exist so the domain-purity rule has something to assert over. |
| B-05 | Catalog database and `CatalogDbContext` | `architecture/solution-layout.md` §6 (B-05) | Platform/Tenancy (catalog) | B-01 | Yes — with B-02, B-03, B-04 | ready | Different project tree from SharedKernel/Architecture.Tests. |
| B-06 | `Aurora.Platform.Tenancy`: `TenantScope`, connection resolver, `ITenantDbContextFactory`, data-source cache | `architecture/solution-layout.md` §6 (B-06) | Platform/Tenancy | B-05 | No — same project as B-07/B-08, which both depend on it anyway | ready | The structural tenancy guarantee (ADR-0007 §4) lives here. |
| B-07 | Tenant provisioning saga | `product/specs/SPEC-001-tenant-provisioning-and-first-login.md`; engineering AC in `architecture/solution-layout.md` §6 (B-07) | Platform/Tenancy | B-06 | Yes — with B-08 (different feature area, same tier) | ready | First task with a PM spec: business rules for who may trigger provisioning, what it must produce, and the audit trail, on top of the saga's engineering AC. |
| B-08 | Migration runner | `architecture/solution-layout.md` §6 (B-08) | Platform/Tenancy | B-06 | Yes — with B-07 | ready | Pure engineering; no product-facing behaviour distinct from what the architect already specified. |
| B-09 | Migration safety and release-gate tests | `architecture/solution-layout.md` §6 (B-09) | Architecture.Tests | B-04 | Yes — with B-05 through B-08, B-12 | ready | |
| B-10 | `Aurora.TestKit`: `TwoTenantDatabaseFixture`, `TenantIsolationContract<T>` | `architecture/solution-layout.md` §6 (B-10) | TestKit | B-07 | No — must follow B-07's saga into the same test infrastructure | ready | Every later module's isolation tests depend on this existing first. |
| B-11 | `verify.sh` stages 4–10, `scripts/check-dependencies.sh` | `architecture/solution-layout.md` §6 (B-11) | Build/Tooling | B-02, B-04, B-10 | Yes — with B-12, B-13, B-14 | ready | Stage 10 (coverage floor) is wired but not yet *enforced* — enforcement turns on at Milestone 2 exit, per `product/roadmap.md`. |
| B-12 | `Aurora.Countries.Contracts` + `Aurora.Countries.Hosting` | `architecture/solution-layout.md` §6 (B-12) | Countries (tier 0) | B-03 | Yes — with B-04 through B-09 | ready | Only depends on SharedKernel; can proceed independently of the whole Tenancy branch. |
| B-13 | Country Package installer | `architecture/solution-layout.md` §6 (B-13) | Countries | B-07, B-12 | Yes — with B-10, B-11 | ready | Needs both the saga (to install during provisioning) and the contracts/hosting mechanism. |
| B-14 | `scripts/new-module.sh` and module template | `architecture/solution-layout.md` §6 (B-14) | Build/Tooling | B-10 | Yes — with B-11, B-12, B-13 | ready | Needed before B-15.1 so the Organization module starts from the generated template, per the architect's own dependency. |
| B-15.1 | Organization module: `Company` domain + application (create, list; permission-gated) | `product/specs/SPEC-002-company-create-and-list.md` | Organization (tier 2) | B-06, B-10, B-13, B-14 | No — B-15.2/.3 build directly on this | ready | Domain invariants (BR-2), the create/list command+query handlers with the permission check (BR-3/BR-4), audit annotation and the outbox event raised as a domain event (BR-5/BR-6). No HTTP/UI yet. |
| B-15.2 | Organization module: `Company` infrastructure, `/api/v1/companies` endpoints, audit + outbox wiring, two-tenant isolation tests | `product/specs/SPEC-002-company-create-and-list.md` | Organization / Aurora.Web (API) | B-15.1 | No | ready | This is where AC-4, AC-5, AC-8 and AC-9 (the isolation-contract pattern, including the deliberate mis-route test) actually get proven end to end against a real PostgreSQL database. |
| B-15.3 | Blazor UI: Company create form + list | `product/specs/SPEC-002-company-create-and-list.md` (UX reference section) + `DESIGN-001` | Aurora.Web (UI) | B-15.2, DESIGN-001 | No | draft | Blocked on `DESIGN-001` per this role's rule that a UI task needs a screen spec. SPEC-002's own UX reference section is enough to unblock `DESIGN-001` quickly (it is not starting from nothing). |
| DESIGN-001 | Screen spec: Company — create form and list (minimal) | — (ui-designer deliverable) | Design | — | Yes | draft | **For the ui-designer.** SPEC-002's "UX reference" section already specifies the single field, the two list columns, and which existing components apply (`components.md` §§1, 2, 9, 17) — this task is to turn that into a short, formal screen spec so `B-15.3` can move to `ready`. Deliberately small; do not scope in the full data-grid or the company switcher (out of scope per SPEC-002). |
| GRID-001 | Data-grid performance spike: 150,000 rows × 40 columns, `QuickGrid` + `Virtualize` over a real SignalR circuit | `product/roadmap.md` Milestone 1.5 | Aurora.Web (UI) / Architecture | B-15 (gate — see below) | Yes — with nothing else scheduled at M1.5 yet | draft | Per `HUMAN_INBOX.md` 2026-09-11 Q9 and ADR-0024. Time-boxed; produces a recorded ADR-0024 confirmation or a Radzen-fallback amendment. Architect + senior developer collaboration; move to `ready` once Milestone 1 (all of B-01…B-15.2, plus B-15.3 if the screen spec lands in time) is green — the roadmap's hard gate applies here too, since this becomes real, shipped UI. |
| GRID-002 | Grid chrome: saved views, column settings, pinning, filter chips, bulk-action bar | `product/roadmap.md` Milestone 1.5; `docs/design/components.md` §8 | Aurora.Web (UI) | GRID-001 | No | draft | The architect's flagged "largest single UI cost" — kept as its own line item deliberately, never to be absorbed into a later screen's task. |
| FOLLOWUP-001 | Per-tenant database login roles (`aurora_app_<tenantKey>`) | ADR-0007 §3.5 stage 2 | Platform/Tenancy | B-06, B-07 | Yes | draft | Must land before the first paying customer, per `product/roadmap.md`; not a Milestone 1 blocker (stage 1 shared-role credentials are acceptable until then). |
| FOLLOWUP-002 | Compiled EF models | ADR-0003 (referenced in `architecture/solution-layout.md` §2.1) | Infrastructure/EF | B-01 | Yes | draft | Must land before first release, per the architect's follow-up list. |
| FOLLOWUP-003 | Operator console over the job queue | `architecture/modules.md` (Jobs, tier 1) | Platform/Jobs | B-08 | Yes | draft | Non-blocking; scoped once the fleet is large enough that hand-debugging the job queue stops working. |
| FOLLOWUP-004 | Enforce the 80% domain-coverage floor in `verify.sh` stage 10 | `architecture/testing-strategy.md` §12; `architecture/solution-layout.md` §5.2 stage 10 | Build/Tooling | B-11 | Yes | draft | Coverage is *collected* from B-11 onward; this task flips it from reported to enforced. Scheduled at Milestone 2 exit per `product/roadmap.md` — do not enable earlier, since Milestone 1's domain surface (SharedKernel + one minimal aggregate) is too small to make the floor meaningful. |
| FOLLOWUP-005 | xUnit v3 evaluation | `architecture/dependencies.md` §3 | Testing/Tooling | B-04 | Yes | draft | Opportunistic; not urgent while xunit 2.9.3 is current and supported. |
| FOLLOWUP-006 | Confirm `Bogus`'s exact licence text | `architecture/dependencies.md` §3 | Dependencies | — | Yes | draft | NuGet exposes a licence *file*, not an SPDX expression, for `Bogus`. Must be read and recorded verbatim in `dependencies.md` before first use — expected around Milestone 4/5's performance guard-rail tests, not before. |
| FOLLOWUP-007 | Per-tenant MFA policy (today: opt-in per user only) | ADR-0009 Consequences | Platform/Identity | B-06 | Yes | draft | Must land before any tenant with a compliance requirement for mandatory MFA; not a Milestone 1 requirement. |
| FOLLOWUP-008 | Quarterly single-tenant restore drill | ADR-0007 §11.2 | Operations/Tenancy | B-07 | Yes | draft | An ongoing operational task, not a one-time development task — the first drill is scheduled once the first tenant holds real (test) data, per `product/roadmap.md`. |
