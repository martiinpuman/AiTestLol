# Backlog

Owned by the project-manager. The orchestrator updates the Status column as work moves.

Statuses: `draft`, `ready`, `in-progress`, `review`, `changes-requested`, `done`, `blocked`.

**How to read "ready" here.** Every row below marked `ready` is fully specified and scoped to fit one developer session, per the project-manager role's definition of ready. The **Depends on** column is authoritative for actual dispatch order — most rows below depend on `B-01` directly or transitively, and since nothing is `done` yet at bootstrap, only `B-01` is literally unblocked right now. The rest are marked `ready` (rather than `draft`) because their specs are complete and nothing further needs grooming before a developer can pick them up **the moment their dependency reaches `done`** — this is what "keep at least 5 ready tasks ahead of the developers" (the project-manager role) means in practice: the queue never runs dry waiting on the project-manager. The orchestrator should not dispatch a `ready` task ahead of an incomplete dependency; it should treat this table as the next several waves of work, groomed in advance.

**The new Tier column** names the review depth from `CLAUDE.md`'s Review tiers table (Full / Standard / Light) for every task that ships code. It is not the module-dependency "tier" used in `architecture/modules.md` (tier 0 kernel, tier 1 platform, …) — a different axis, same word; do not confuse them. `—` marks a row that is not developer-reviewed code (a ui-designer deliverable).

Bootstrap tasks `B-01` … `B-15` are the architect's walking-skeleton sequence (`docs/architecture/solution-layout.md` §6), reproduced here with real dependency ordering and, where the task is pure platform/engineering scaffolding with no product-facing "problem and user," the architect's own acceptance criteria stand as the spec (see Spec column) rather than a duplicate product document — a PM spec exists only where the task has real business/tenancy/permission/audit shape a developer would otherwise have to guess at (`B-07`, `B-15`). `B-15` itself was split into three backlog rows (`B-15.1`/`.2`/`.3`) because, as specified, it bundled domain+application+infrastructure+API+UI+two-tenant testing into one task that did not fit a single session.

**2026-09-11, second split.** Measured cycle time on the first three completed tasks (B-01 44 min, B-02 74 min, B-03 96 min) showed the same pattern each time: a large task fails half as expensively when it is half the size, because the rejection round repeats the whole task, not the broken part. The architect's v3 pass over `solution-layout.md` §6 (2026-09-11) also **widened** the B-03 … B-08 acceptance-criteria rows to the ADR sections they actually implement — several of them (B-06, B-07, B-08) were already far past the "under ~400 changed lines, one developer, one session" rule before the widening, and further past it after. This pass splits **B-06** (tenancy core), **B-07** (provisioning saga — the architect's own flag as "the largest and riskiest row in the milestone," since its old row was satisfiable by a saga that could silently adopt another tenant's database), **B-08** (migration runner) and **B-13** (Country Package installer) into `.1`/`.2`/… rows, using the same dotted-suffix convention as `B-15`. `B-04`, `B-05` and `B-12` are also past the sizing rule but are not split here — four developers are live on `B-04`, `B-05`, `B-12` and a tooling follow-up right now, and splitting a task out from under its author would waste the work already in flight; only their **Tier** cell and a short **Notes** clause pointing at the widened ADR sections were added. Every split below tries to land the genuinely dangerous mechanism in its own task — so it carries the full weight of a Full-tier review — while isolating everything else that does not need that weight, per `CLAUDE.md`'s note that "a split that produces two Full-tier halves saves less than one that isolates the risky part into a Full-tier task and leaves the rest Standard." In tenancy, migrations and Country Packages there was no Standard-tier meat to isolate — `CLAUDE.md`'s own Full-tier list names all three categories outright — so those splits are Full tier end to end; the saving there is purely in size, not in review depth per task. `B-15` remains the milestone gate: no business module starts before all of `B-01` … `B-15.2` (plus `B-15.3` if its screen spec lands in time) is green, unchanged from the architect's sequence.

| ID | Title | Spec | Module | Tier | Depends on | Parallel-safe | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| B-01 | Solution skeleton and build-wide settings | `architecture/solution-layout.md` §6 (B-01) | Solution/Build | Light | — | Yes — nothing else can start until this lands, but nothing blocks *it* | done | Pure engineering scaffolding; architect's AC is the spec. Everything else in this table depends on it, directly or transitively. |
| B-02 | `scripts/verify.sh` stages 0–3, 6 and 11 | `architecture/solution-layout.md` §6 (B-02), §5 | Build/Tooling | Light | B-01 | Yes — with B-03, B-05 | done | Touches `scripts/` only; no file overlap with B-03/B-05. Stage 6 (unit tests, with the executed-test floor) moved here from B-11 in solution-layout.md v3, after this task's own review found a solution-wide failure no other planned stage would have caught — already merged with stage 6 included. Title corrected here from an earlier "stages 0–3 and 11" that predated the v3 move; the shipped script already has stage 6, only this row's text was stale. |
| B-03 | `Aurora.SharedKernel` (Money, Quantity, Percentage, DateRange, ids, `Result`) | `architecture/solution-layout.md` §6 (B-03) | SharedKernel (tier 0) | Full | B-01 | Yes — with B-02, B-05 | done | Foundational value objects; every later module reads this but nothing here reads them back. Widened in solution-layout.md v3 to ADR-0021 §6's exact-allocation postcondition (parts sum exactly to the total, every part a whole number of minor units, for a genuinely arbitrary weight generator) — already satisfied: 208 tests, done. |
| B-04 | `Aurora.Architecture.Tests` first rule set | `architecture/solution-layout.md` §6 (B-04) | Architecture.Tests | Full | B-01, B-03 | Yes — with B-05, B-12 | ready | Needs B-03 to exist so the domain-purity rule has something to assert over. Widened in solution-layout.md v3 to ADR-0007 §12.3's five named tenancy rules and a precise scope for the floating-point rule (local variables and casts inside a method body, not just member signatures) — currently in flight; the developer should re-read the current row text, not an earlier cached version. |
| B-05 | Catalog database and `CatalogDbContext` | `architecture/solution-layout.md` §6 (B-05) | Platform/Tenancy (catalog) | Full | B-01 | Yes — with B-02, B-03, B-04 | ready | Different project tree from SharedKernel/Architecture.Tests. Widened in solution-layout.md v3 to the full `catalog.tenant.state` enum (later steps depend on states this task creates) and a mechanical test that the catalog holds no tenant business data, with `catalog.identity_user`'s email as the one recorded exception — currently in flight; re-read the current row text. |
| B-06.1 | `Aurora.Platform.Tenancy`: connection resolver and pool settings | `architecture/solution-layout.md` §6 (B-06); ADR-0007 §3.5, §5.2 | Platform/Tenancy | Full | B-05 | No — B-06.2/.3 build directly on it | ready | Implements `ITenantConnectionResolver` — the only code that builds a tenant connection string, reading `catalog.tenant` + `catalog.database_cluster`, cached 60 s in `HybridCache` under a tenant-prefixed key and invalidated on tenant state change (proven by suspending a tenant and asserting the next resolve does not serve the cached row) — plus the §5.2 pool-settings table (Minimum Pool Size 0, Maximum 10 Web / 5 Worker, 30 s idle lifetime, etc.). Does not yet build `TenantScope` or the factory; first task in the chain, nothing else in this split can start before it. |
| B-06.2 | `Aurora.Platform.Tenancy`: bounded data-source cache and the connected-database identity check | `architecture/solution-layout.md` §6 (B-06); ADR-0007 §5.1, §4.3 | Platform/Tenancy | Full | B-06.1 | No — B-06.3 depends on it | ready | The bounded LRU `ITenantDataSourceCache` (512 entries default, configurable; eviction disposes a data source asynchronously only after in-flight connections drain — proven by a test that evicts under load and asserts no in-flight query is killed) and, in the same `NpgsqlDataSourceBuilder` physical-connection initializer, the §4.3 identity check: once per physical connection, compares `platform.tenant_identity.tenant_id` to the expected `TenantId`; a mismatch throws `TenantRoutingViolationException`, fails the request, alerts and marks the tenant `SchemaBlocked` — proven by a deliberate mis-route test. **The single riskiest mechanism in the B-06 split; give it the closest reading in review.** |
| B-06.3 | `Aurora.Platform.Tenancy`: `TenantScope`, `ITenantDbContextFactory` and the structural no-`DbContext`-without-a-tenant guarantee | `architecture/solution-layout.md` §6 (B-06); ADR-0007 §3.4, §4.1, §4.2 | Platform/Tenancy | Full | B-06.2 | No — B-07's and B-08's chains both wait on this | ready | `TenantScope` (sealed, no public constructor, `IsActive` false after disposal so a reused scope throws) and `ITenantDbContextFactory<TContext>` — no tenant `DbContext` is ever registered in DI, none has a public/protected constructor, nothing outside this assembly implements the factory interface (also asserted by B-04's fitness tests; coordinate on the fixture). `ITenantScopeAccessor.Current` is registered only in `Aurora.Web` and throws `NoTenantResolvedException` off the ambient path — a test asserts resolving it from the worker composition root fails. Depends on B-06.2 so `CreateAsync` proves the guarantee against a real, identity-checked data source, not a stub. Everything downstream — B-07, B-08, B-09's tenancy-adjacent rules, every module — ultimately depends on this row. |
| B-07.1 | Provisioning saga: reserve, create (safe-adoption rule) and harden the tenant database | `product/specs/SPEC-001-tenant-provisioning-and-first-login.md` (BR-1, BR-2, AC-2); `architecture/solution-layout.md` §6 (B-07); ADR-0007 §8 steps 1–3, reaper, compensation guard | Platform/Tenancy | Full | B-06.3 | Yes — with B-08.1 (both start once B-06.3 merges; different feature areas) | ready | Covers who may trigger provisioning and what a request must specify (SPEC-001 BR-1/BR-2), and AC-2 (unauthorized attempt creates no tenant, database or catalog row). Builds the saga's own executor shell (`catalog.tenant_provisioning`/`tenant_provisioning_step` state, the reaper re-leasing an expired non-terminal run from `current_step`, `ProvisioningFailed` + page after 5 attempts) and the compensation guard (`DROP DATABASE` only from the operator console, only for a `ProvisioningFailed` tenant, only after re-reading `platform.tenant_identity` and confirming the match). **The highest-risk single task in this whole split**: step 2's SQLSTATE `42P04` handling must never adopt a database it cannot prove is ours — owned by `aurora_migrator` and either empty or already stamped with this `tenant_id` — proven by replaying against a database stamped for a *different* tenant and asserting refusal, not adoption. Per STATE.md's standing warning, the reviewer should spend the bulk of a Full review here. |
| B-07.2 | Provisioning saga: stamp tenant identity and migrate schema | `architecture/solution-layout.md` §6 (B-07); ADR-0007 §8 steps 4–5, §7.5 | Platform/Tenancy | Full | B-07.1 | Yes — with B-08.1/.2 (different feature area, same tier) | ready | StampIdentity (creates schema `platform`, creates and inserts `platform.tenant_identity`, fails loudly if an existing row names another tenant) and MigrateSchema (runs all module migrations to `CurrentSchemaVersion`). Publishes the two schema-version constants (`CurrentSchemaVersion`, `MinimumSupportedSchemaVersion`) that B-08.3's skew check later consults. Uses a direct admin/migrator connection built the same way as B-07.1's steps 2–3 — not `ITenantConnectionResolver`, which is for `aurora_app` traffic (ADR-0007 §3.5) and is not yet meaningful before a tenant is migrated. First point in the saga where a Company row can exist, which is what frees B-07.3 and B-13.1 to proceed in parallel. |
| B-07.3 | Provisioning saga: seed platform data | `product/specs/SPEC-001-tenant-provisioning-and-first-login.md` (BR-3, seeding half); `architecture/solution-layout.md` §6 (B-07); ADR-0007 §8 step 6 | Platform/Tenancy | Full | B-07.2 | Yes — with B-13.1/.2 (both branch from B-07.2; seeding vs. package install do not depend on each other) | ready | Default Company, seeded system roles and permission catalogue (ADR-0010), the one administrator Membership scoped to every Company in the tenant (`null` company scope, ADR-0010 rule 4), tenant settings — natural keys with `on conflict do nothing` for idempotency. |
| B-07.4 | Provisioning saga: install packages, register routing, announce — prove the saga end to end | `product/specs/SPEC-001-tenant-provisioning-and-first-login.md` (BR-3 in full, BR-4/AC-3, BR-5/AC-4, BR-6/AC-5, BR-7/AC-6, AC-7); `architecture/solution-layout.md` §6 (B-07); ADR-0007 §8 steps 7–9 | Platform/Tenancy | Full | B-07.3, B-13.2 | No — the join point of the B-07/B-13 branch; only B-10 follows it | ready | InstallPackages (calling B-13.2's installer), RegisterRouting, Announce to the catalog outbox. Closes out SPEC-001: BR-4/AC-3 (kill the run after *any* of the nine steps, resume, exactly one of everything — the first point this test can span the whole saga), BR-5/AC-4 (an unresolvable package id fails loudly, tenant never reaches `Active`), BR-7/AC-6 (audit on both sides of the tenant boundary), AC-7 (under 60 s with no injected failure). AC-5 (first login proves isolation) is exercised here with a minimal two-tenant setup; its exhaustive proof is B-10's job once the `TenantIsolationContract` exists. |
| B-08.1 | Migration runner: claiming, leasing and per-tenant migration execution | `architecture/solution-layout.md` §6 (B-08); ADR-0007 §7.3, §7.4 (resumability clause) | Platform/Tenancy | Full | B-06.3 | Yes — with B-07.1 (both start once B-06.3 merges) | ready | `FOR UPDATE SKIP LOCKED` claiming with a 5-minute heartbeated lease; migration order within one tenant (`platform` → module schemas per `modules.md` dependency order → `pkg_*`); the session-level advisory lock over a direct (non-PgBouncer) `aurora_migrator` connection; idempotent re-run (skip `Succeeded`, retry `Failed` while `attempts < 3`, re-lease expired leases). Does not yet handle a fleet-wide bad release — that is B-08.2. Open question for the architect below on whether this genuinely needs the full B-06 guarantee or only B-06.1's connection resolver; kept at the conservative dependency here. |
| B-08.2 | Migration runner: quarantine, failure budget and waves | `architecture/solution-layout.md` §6 (B-08); ADR-0007 §7.4 (quarantine, budget, waves) | Platform/Tenancy | Full | B-08.1 | Yes — with B-07.2/.3, B-13.1/.2 (different feature area) | ready | A failing tenant is quarantined (`Quarantined` + `catalog.tenant.state = SchemaBlocked`) while the run continues serving every other tenant; the failure budget (default 0.5% of the wave, minimum 3) halts the run and pages; wave progression `internal → canary → early → general` with a soak gate between waves. |
| B-08.3 | Migration runner: schema-version skew checks at scope open | `architecture/solution-layout.md` §6 (B-08); ADR-0007 §7.5 | Platform/Tenancy | Full | B-08.1, B-06.3 | No — edits the same scope-factory code B-06.3 shipped; sequence after B-08.1 | ready | `ITenantScopeFactory` checks on every scope open, using the constants B-07.2 published: too old (`< MinimumSupportedSchemaVersion`) fails the scope open, sets `SchemaBlocked`, alerts; too new (code rolled back) fails the scope open on this instance and drains it. **This task edits `Aurora.Platform.Tenancy` (the scope factory), not a migration-runner-only project** — flagged for the architect to confirm which project owns the two version constants before dispatch. |
| B-09 | Migration safety and release gate tests | `architecture/solution-layout.md` §6 (B-09) | Architecture.Tests | Full | B-04 | Yes — with B-05, B-06.\*, B-07.\*, B-08.\*, B-12 (different module) | ready | Categories `Expand`/`Contract`/`DataOnly`; SQL scan for destructive statements; release gate failing when an Expand and its Contract ship together; `CREATE INDEX` without `CONCURRENTLY` rejected. |
| B-10 | `Aurora.TestKit`: `TwoTenantDatabaseFixture`, `TenantIsolationContract<T>` | `architecture/solution-layout.md` §6 (B-10) | TestKit | Full | B-07.4 | No — must follow the completed saga into the same test infrastructure | ready | Every later module's isolation tests depend on this existing first. Depends on B-07.4 specifically because "through the **real** saga" means all nine steps, including package install and routing. |
| B-11 | `verify.sh` stages 4, 5, 7–10, `scripts/check-dependencies.sh` | `architecture/solution-layout.md` §6 (B-11) | Build/Tooling | Light | B-02, B-04, B-10 | Yes — with B-12, B-13.\*, B-14 | ready | Stage 6 belongs to B-02 (already merged); this task extends its self-test harness, it does not re-implement unit-test execution. Title corrected here from an earlier "stages 4–10" that predated the v3 stage-6 move. Stage 10 (coverage floor) is wired but not yet *enforced* — enforcement turns on at Milestone 2 exit, per `product/roadmap.md`. |
| B-12 | `Aurora.Countries.Contracts` + `Aurora.Countries.Hosting` | `architecture/solution-layout.md` §6 (B-12) | Countries (tier 0) | Full | B-03 | Yes — with B-04 through B-09 | ready | Only depends on SharedKernel; can proceed independently of the whole Tenancy branch. |
| B-13.1 | Country Package installer: compatibility and conflict pre-flight checks | `architecture/solution-layout.md` §6 (B-13); ADR-0008 §5.1 steps 1–5 | Countries | Full | B-12, B-07.2 | Yes — with B-07.3 (both branch from B-07.2) | ready | The refusal path: resolve the highest compatible version from the package catalogue; verify the ECDSA P-256 signature (§9.3), refusing unsigned/untrusted outside Development; check `coreContractRange` against the running core contract version, naming both versions on refusal; check `dependsOn`/`conflictsWith`; check slot conflicts per company. Every refusal is RFC 9457 Problem Details. Needs a migrated tenant database to install into or refuse against (B-07.2) and the manifest/loading machinery (B-12). |
| B-13.2 | Country Package installer: schema migration with the core-DDL-hash guard, first install | `architecture/solution-layout.md` §6 (B-13); ADR-0008 §5.1 steps 6–9, §4.1 R2, §10 | Countries | Full | B-13.1 | No — B-07.4 and B-13.3 both wait on it | ready | `CREATE SCHEMA pkg_<key>`; the core-DDL-hash guard (snapshot `information_schema`/`pg_indexes` for every non-`pkg_` schema before and after package migrations, abort and roll back on any difference — proven by a deliberately core-touching test package, `Install_does_not_alter_core_ddl`); contribution seeding tagged `source='Package'` (first install only — nothing to merge yet, since `tenant_modified_at` is always null here); capability registration, `catalog.installed_package`, `CountryPackageInstalled`. Also proves `Install_into_a_live_tenant_with_existing_data` (a tenant that already has a different package installed and posted documents). This is the piece B-07.4 actually calls — keep its public surface to exactly what step 7 needs. |
| B-13.3 | Country Package installer: upgrade three-way merge, deactivate and purge | `architecture/solution-layout.md` §6 (B-13); ADR-0008 §4.2, §5.2 (packages half), §5.3 | Countries | Full | B-13.2 | Yes — with B-07.4 (both branch from B-13.2; upgrade/uninstall vs. saga finalization) | ready | The three-way merge on upgrade (untouched package rows take the new value; package rows the tenant has edited produce a `PackageUpdatePending` resolved only via `Package.Manage`; tenant- and core-owned rows are never touched) and deactivate/purge (purge blocked if any posted document references package data, naming the referencing document types in the response). Not needed for the walking skeleton's own install-only path (B-07.4, B-15.1 depend on B-13.2, not this row) — free to build in parallel. **Open question for the architect:** §5.2's per-tenant package-upgrade-plan computation reads like fleet migration-runner work (B-08), not installer work; confirm which project owns it before this task is dispatched. |
| B-14 | `scripts/new-module.sh` and module template | `architecture/solution-layout.md` §6 (B-14) | Build/Tooling | Light | B-10 | Yes — with B-11, B-12, B-13.\* | ready | Generates the four projects, the `DbContext` with `HasDefaultSchema`, the DI extension, the unit and integration test projects and a `TenantIsolationContract` subclass; the generated module passes `verify.sh` unchanged. Needed before B-15.1 so the Organization module starts from the generated template. |
| B-15.1 | Organization module: `Company` domain + application (create, list; permission-gated) | `product/specs/SPEC-002-company-create-and-list.md` | Organization (tier 2) | Standard | B-06.3, B-10, B-13.2, B-14 | No — B-15.2/.3 build directly on this | ready | Domain invariants (BR-2), the create/list command+query handlers with the permission check (BR-3/BR-4), audit annotation and the outbox event raised as a domain event (BR-5/BR-6). No HTTP/UI yet. Depends on B-13.2 (install), not B-13.3 (upgrade/uninstall) — the walking skeleton only ever installs one package once. |
| B-15.2 | Organization module: `Company` infrastructure, `/api/v1/companies` endpoints, audit + outbox wiring, two-tenant isolation tests | `product/specs/SPEC-002-company-create-and-list.md` | Organization / Aurora.Web (API) | Standard | B-15.1 | No | ready | This is where AC-4, AC-5, AC-8 and AC-9 (the isolation-contract pattern, including the deliberate mis-route test) actually get proven end to end against a real PostgreSQL database. |
| B-15.3 | Blazor UI: Company create form + list | `product/specs/SPEC-002-company-create-and-list.md` (UX reference section) + `DESIGN-001` | Aurora.Web (UI) | Standard | B-15.2, DESIGN-001 | No | draft | Blocked on `DESIGN-001` per this role's rule that a UI task needs a screen spec. SPEC-002's own UX reference section is enough to unblock `DESIGN-001` quickly (it is not starting from nothing). |
| DESIGN-001 | Screen spec: Company — create form and list (minimal) | — (ui-designer deliverable) | Design | — | — | Yes | draft | **For the ui-designer.** SPEC-002's "UX reference" section already specifies the single field, the two list columns, and which existing components apply (`components.md` §§1, 2, 9, 17) — this task is to turn that into a short, formal screen spec so `B-15.3` can move to `ready`. Deliberately small; do not scope in the full data-grid or the company switcher (out of scope per SPEC-002). |
| GRID-001 | Data-grid performance spike: 150,000 rows × 40 columns, `QuickGrid` + `Virtualize` over a real SignalR circuit | `product/roadmap.md` Milestone 1.5 | Aurora.Web (UI) / Architecture | Standard | B-15 (gate — see below) | Yes — with nothing else scheduled at M1.5 yet | draft | Per `HUMAN_INBOX.md` 2026-09-11 Q9 and ADR-0024. Time-boxed; produces a recorded ADR-0024 confirmation or a Radzen-fallback amendment. Architect + senior developer collaboration; move to `ready` once Milestone 1 (all of B-01…B-15.2, plus B-15.3 if the screen spec lands in time) is green — the roadmap's hard gate applies here too, since this becomes real, shipped UI. |
| GRID-002 | Grid chrome: saved views, column settings, pinning, filter chips, bulk-action bar | `product/roadmap.md` Milestone 1.5; `docs/design/components.md` §8 | Aurora.Web (UI) | Standard | GRID-001 | No | draft | The architect's flagged "largest single UI cost" — kept as its own line item deliberately, never to be absorbed into a later screen's task. |
| FOLLOWUP-001 | Per-tenant database login roles (`aurora_app_<tenantKey>`) | ADR-0007 §3.5 stage 2 | Platform/Tenancy | Full | B-06.1, B-07.1 | Yes | draft | Must land before the first paying customer, per `product/roadmap.md`; not a Milestone 1 blocker (stage 1 shared-role credentials are acceptable until then). Depends on B-06.1 ("stage 2 changes one class and zero callers" — that class is the connection resolver) and B-07.1 (the `GRANT CONNECT` statements it will change live in HardenDatabase). |
| FOLLOWUP-002 | Compiled EF models | ADR-0003 (referenced in `architecture/solution-layout.md` §2.1) | Infrastructure/EF | Light | B-01 | Yes | draft | Must land before first release, per the architect's follow-up list. A performance cache over an unchanged model, not a behaviour change. |
| FOLLOWUP-003 | Operator console over the job queue | `architecture/modules.md` (Jobs, tier 1) | Platform/Jobs | Standard | B-08.1 | Yes | draft | Non-blocking; scoped once the fleet is large enough that hand-debugging the job queue stops working. Depends on B-08.1's claiming/leasing pattern, which the job queue's own claiming mirrors. |
| FOLLOWUP-004 | Enforce the 80% domain-coverage floor in `verify.sh` stage 10 | `architecture/testing-strategy.md` §12; `architecture/solution-layout.md` §5.2 stage 10 | Build/Tooling | Light | B-11 | Yes | draft | Coverage is *collected* from B-11 onward; this task flips it from reported to enforced. Scheduled at Milestone 2 exit per `product/roadmap.md` — do not enable earlier, since Milestone 1's domain surface (SharedKernel + one minimal aggregate) is too small to make the floor meaningful. |
| FOLLOWUP-005 | xUnit v3 evaluation | `architecture/dependencies.md` §3 | Testing/Tooling | Light | B-04 | Yes | draft | Opportunistic; not urgent while xunit 2.9.3 is current and supported. |
| FOLLOWUP-006 | Confirm `Bogus`'s exact licence text | `architecture/dependencies.md` §3 | Dependencies | Light | — | Yes | draft | NuGet exposes a licence *file*, not an SPDX expression, for `Bogus`. Must be read and recorded verbatim in `dependencies.md` before first use — expected around Milestone 4/5's performance guard-rail tests, not before. |
| FOLLOWUP-007 | Per-tenant MFA policy (today: opt-in per user only) | ADR-0009 Consequences | Platform/Identity | Full | B-06.3 | Yes | draft | Must land before any tenant with a compliance requirement for mandatory MFA; not a Milestone 1 requirement. |
| FOLLOWUP-008 | Quarterly single-tenant restore drill | ADR-0007 §11.2 | Operations/Tenancy | Full | B-07.4 | Yes | draft | An ongoing operational task, not a one-time development task — the first drill is scheduled once the first tenant holds real (test) data, per `product/roadmap.md`. |
